python-telegram-bot==20.7
python-dotenv==0.19.0
# aiohttp==3.9.1 # Убираем снова, т.к. не нашли использования
aiofiles==23.2.1 # Возвращаем, нужен для асинхронной работы с файлами
pytz==2024.1
APScheduler==3.10.4
requests==2.31.0
# flask[async]==2.0.1 # Убираем Flask
# flask-cors==3.0.10 # Убираем Flask-CORS
# werkzeug==2.0.1 # Убираем зависимость Flask
# asgiref==3.7.2 # Обычно ставится с FastAPI/Uvicorn
# hypercorn==0.15.0 # Заменим на uvicorn для консистентности с другими сервисами
psycopg2-binary==2.9.9 

# Добавляем зависимости FastAPI
fastapi==0.111.0 # Используем актуальную версию
uvicorn[standard]==0.29.0 # Используем актуальную версию 